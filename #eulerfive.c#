#include <stdio.h>

int main(){
  int[] primes = new int[];
  for(int i = 20;i >= 1;i--){
    int temp = i;
    for(int n = 2;n <= temp;n++){
      while(temp % n == 0){
	temp /= n;
        try{
	  primes.pop
	}
      }
    }
  }
  printf("%d",num);
  return num;
}
